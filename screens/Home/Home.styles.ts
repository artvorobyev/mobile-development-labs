import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
  input: {
    height: 40,
    marginVertical: 12,
    borderWidth: 1,
    padding: 10,
    width: '100%',
  },
  buttonView: {
    width: '100%',
    justifyContent: 'center',
  },
  phoneText: {
    width: '100%',
    textAlign: 'center',
    marginBottom: 30,
  },
});
